import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  ScrollView,
  TouchableOpacity,
  Image,
  Modal,
  SafeAreaView,
  StatusBar,
} from "react-native";

const Profile = () => {
  return <div>Profile</div>;
};

export default Profile;
